# Dynamic Programming

Dynamic Programming (DP, 动态规划) 是一种把复杂问题分解成相对简单的自问题，并利用存储重用已计算结果来规避重复计算的算法思想



## 核心思想：用空间换时间

假设要计算 1 + 1 + 1 + 1 + 1

1. 一步步计算出答案是5
2. 如果此时在左边再加一个 1 + , 即 1 + 1 + 1 + 1 + 1 + 1，不需要从头再加一遍，而是直接在前面算出的5上加1得到6

这种“记住前面算过的结果，并在其基础上推导新结果”的机制，就是动态规划的本质

## 使用条件

并不是所有问题都能用动态规划解决，一个问题必须满足以下特征：

- 重叠自问题(Overlapping Subproblems)：大问题可以拆分成自问题，且这些自问题在递归求解过程中会重复出现
- 最优子结构(Optimal Substructure)：大问题的最优解可以由其子问题的最优解直接推导组合出来

无后效性（补足性质）：某状态一旦确定，其后续的变化只与该状态的值有关，不受到达该状态的过程影响

## 动态规划四步求解法

1. 定义状态：确定`dp[i]`或`dp[i][j]` 表达的明确物理含义（如：“到达第`i`阶楼梯的最优解）
2. 推导状态转移方程：找出大问题与子问题之间的数学逻辑关系（如`dp[i] = dp[i-1] + dp[i-2]`）
3. 确定边界条件：初始化最基础的已知状态（如：`dp[0] = 0`, `dp[1] = 1`）
4. 确定计算顺序：通常按照从小到大的顺序递推填充DP表，确保计算当前状态时所需的前置状态已存有结果

---

示例：斐波那契数列

计算第n个斐波那契数：\(F(n) = F(n - 1) + F(n - 2)\)

- 暴力的递归解法：计算\(F(5)\) 需要算\(F(4)\) 和 \(F(3)\)；而计算\(F(4)\)又需要计算\(F(3)\)和\(F(2)\)......\(F(3)\)被重复计算了多次，时间复杂度呈指数级\(O(2^n)\)

- 动态规划解法：建立一个数组（DP表），从\(F(0)\)和\(F(1)\)开始顺推，把计算过的每个结果记在表里：\
\[F(0) = 0, F(1) = 1, F(2) = 1, F(3) = 2, F(4) = 3, F(5) = 5\]

每个状态只计算一次，时间复杂度优化至\(O(n)\)

---

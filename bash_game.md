# Bash Game

巴什博弈，取石子游戏\
游戏规则：有一堆石子，两个人轮流取石子，每次至少取1个，最多取m个。最后一个把石子取完的人获胜

假设：

- 一开始有`n`个石子
- 每次最多取`m`个

决胜的关键在于计算 n (mod m + 1) 的余数：

- 核心原理：无论先手拿取x个(1 <= x <= m)，后手总能选择拿取(m + 1 - x) 个，从而保证两人在这一轮中共同拿走的数量为固定值(m + 1)
- 后手必胜条件：如果 n (mod m + 1) == 0，后手只需保证每次凑成(m + 1) 的倍数，先手必败
- 先手必胜策略：如果 n (mod m + 1) = k != 0，先手第一轮只需拿走k 个，奖剩余数量变为(m + 1)的倍数，之后转换为“后手策略”即可稳赢

## Misère模式

Misère Bash Game 反常巴什博弈\
游戏规则：有一堆石子，两个人轮流取石子，每次至少取1个，最多取m个。最后一个把石子取完的人失败

## 程序实现

```c
#include <stdbool.h>
#include <stdio.h>

// 常规 bash game
// 返回 true 表示先手必胜，false 表示后手必胜
bool isFirstPlayerWinNormal(int n, int m) {
    return (n % (m + 1)) != 0;
}

// 反常规 bash game
```

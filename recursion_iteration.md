# Recursion and Iteration

递归不需要你有任何感性的认识，你不必关注行不行，脑中不需要有函数调用栈，你想不到也不用想\
一个问题只要符合递归的条件，你只需要定义一个足够小、足够明确的问题，然后相信它

从计算模型的角度来看，递归调用都可以用显式栈来模拟\
这句话并不是说所有的递归都能简单地改成一个`stack`，而是所有递归调用都可以通过保存足够的调用状态，用显式数据结构模拟
